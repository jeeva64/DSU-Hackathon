# NOTE: keep streamlit on a tornado-only line. Newer streamlit (>=1.47) pulls
# starlette>=0.46 which conflicts with the backend's fastapi 0.115.x
# (requires starlette<0.42). streamlit 1.40 has st.toggle, column_config,
# cache_data and streamlit.testing v1 AppTest.
streamlit==1.40.0
requests>=2.31
plotly>=5.22
pandas>=2.1
numpy>=1.26